# Repository Guidelines

## 操作边界和安全规则

### 修改前的调查与确认

- 修改前必须阅读相关文件，说明发现的问题、拟采取的方案和影响范围，获得用户明确确认后再执行修改。用户已明确确认的具体方案可直接执行；超出已确认范围时必须重新确认。
- 修改前必须检查 `git status` 和相关差异，包括未跟踪文件。保护用户尚未提交的工作，不得覆盖、撤销或重写已有未提交改动；若与当前任务冲突，先说明并等待用户决定。
- 未经明确确认，不得新增依赖、框架、构建工具或安装软件包。
- 不得自动执行 `git commit`、`git push`、创建 PR 或部署网站。只有用户明确要求相应操作后才能执行；允许修改文件不代表允许这些操作，发布说明也不构成授权。

### 修改范围与资源保护

- 每次只修改完成当前任务所必需的内容。发现其他问题可以报告，不得顺手修改。
- 不进行未经要求的大范围重构、代码格式化、视觉改版或文件整理。
- 除非当前任务明确要求，不得删除或替换已有作品、改变作品排序、重命名资源、修改资源路径或改变视频内容。
- 不得把密码、API Key、Token、私钥或其他敏感信息写入网站或提交到仓库。

### 视觉、响应式与视频约束

- 保持极简、低饱和、克制的视觉风格，保留现有整体版式，避免花哨动画和过度装饰。修改时同时考虑 Desktop 和 Mobile。
- 默认沿用现有 `760px` 响应式断点；确需调整时，必须先解释原因和影响范围，并获得明确确认。
- 视频修改优先考虑移动端加载性能，避免页面加载时预加载大量视频。不得擅自改变点击播放逻辑、视频内容或媒体资源结构。
- 代码风格要求仅适用于当前新增或修改的代码，不得因此重新格式化整个文件。

### 修改后的检查与汇报

- 检查 `git diff`、`git diff --check` 和 `git status`，并单独读取、核对新增未跟踪文件；普通 `git diff` 不显示未跟踪文件内容。如有暂存改动，也要检查 `git diff --cached`。
- 区分用户原有改动和本次改动，确认本次修改没有超出授权范围。
- 完成后必须汇报：修改了哪些文件、每个文件改了什么、为什么修改、潜在风险、已经实际执行的测试及结果，以及仍需用户手动测试的内容。
- 不得将建议测试、未执行的检查或仅阅读代码的判断描述为已经测试通过；无法验证的内容应明确说明。

## 项目结构和技术说明

### 网站用途与核心文件

这是 BoLang 的静态单页商业影视作品网站，展示导演、摄影、剪辑能力、酒旅与商业品牌项目、业务范围和合作方式。

- `index.html`：页面结构、文案、首页 Showreel、六组作品、服务、设备、预算、联系方式及共用视频弹窗。
- `assets/styles.css`：颜色、字体、布局、响应式规则和视觉效果。
- `assets/site.js`：手机菜单、微信号复制、视频加载与播放交互。
- `assets/*.webp`：作品封面图片。
- `videos/*.bin`：视频二进制分片。
- `上传说明.txt`：GitHub Pages 发布参考；`.nojekyll` 为发布辅助标记。

当前没有后端、数据库、依赖清单、构建系统、自动测试目录、格式化工具或 lint 工具。作品信息直接写在 HTML 中。

### 本地预览与媒体机制

无需构建。若已安装 Python，可在项目根目录运行 `python -m http.server 8000 --bind 127.0.0.1`，访问 `http://127.0.0.1:8000/`，使用 Ctrl+C 停止。通过 HTTP 预览视频加载，不以直接打开 `file://` 页面作为播放验证。若未安装 Python，先报告，不自行安装。

资源使用相对路径，例如 `./assets/showreel.webp`。视频按钮的 `data-video-parts` 保存以 `|` 分隔的有序分片路径，例如 `showreel.part-00.bin`。点击后，JavaScript 下载该视频全部分片，按顺序组成 MP4 Blob，再交给 `<video>` 播放。维护时保护分片顺序及封面、标题、横竖屏信息之间的对应关系。

### 代码风格与测试

沿用相关代码的现有风格：两空格缩进、语义化 HTML、kebab-case CSS 类名、camelCase JavaScript 变量，以及 JavaScript 单引号和分号。优先复用 CSS 自定义属性。可见文案以中文为主，保留现有适用的英文标签。

没有自动测试框架或覆盖率要求。按改动范围实际检查桌面和手机布局、导航、菜单、复制反馈与键盘操作。涉及视频时，检查横竖屏播放、加载提示、加载中关闭和重新打开其他视频。测试发现的范围外问题应报告，不得自动扩大修改。

### 获得授权后的提交与 PR

历史提交使用简短英文摘要，例如 `Add BoLang portfolio website`，未形成强制命名规范。仅在用户明确要求提交时，使用简洁、说明动作的提交信息。仅在用户明确要求创建 PR 时，描述改动、原因和实际验证结果；视觉改动附适用的桌面与手机截图，有关联 issue 时提供链接。
